import React from 'react';
import RestaurantList from '../components/RestaurantProfileList';

const Home: React.FC = () => {
    return (
        <div>
            <RestaurantList />
        </div>
    );
};

export default Home;